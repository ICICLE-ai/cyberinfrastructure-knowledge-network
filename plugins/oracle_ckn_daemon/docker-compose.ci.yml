services:
  ckn-daemon:
    environment:
      - CKN_KAFKA_BROKER=broker:29092
      - CKN_KAFKA_SECURITY_PROTOCOL=PLAINTEXT
    networks:
      - ckn-network

networks:
  ckn-network:
    external: true
